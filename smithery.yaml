# Smithery MCP Registry Configuration
# https://smithery.ai
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({ command: "aceternity-mcp-server", args: [] })
